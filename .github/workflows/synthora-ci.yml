name: Synthora CI

on:
  push:
    paths: ["synthora/**", ".github/workflows/synthora-ci.yml"]
  pull_request:
    paths: ["synthora/**", ".github/workflows/synthora-ci.yml"]

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: synthora
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          python-version: "3.12"
      - name: Sync workspace
        run: uv sync
      - name: Lint
        run: uv run ruff check .
      - name: Pytest
        run: uv run pytest -q

  web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: synthora/apps/web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: synthora/apps/web/package-lock.json
      - name: Install
        run: npm ci
      - name: Unit tests
        run: npm test
      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium
      - name: E2E (API mocks)
        run: npm run test:e2e

  smoke:
    runs-on: ubuntu-latest
    needs: [test]
    defaults:
      run:
        working-directory: synthora
    steps:
      - uses: actions/checkout@v4
      - name: Compose smoke
        env:
          SYNTHORA_AUTH_MODE: none
          SYNTHORA_SECRET_KEY: ci-smoke-secret-key-not-for-prod-32
          SYNTHORA_CHECKPOINT_BACKEND: postgres
        run: bash scripts/smoke.sh
        # Compose builds are heavy; allow retries on flaky pulls.
        timeout-minutes: 45
