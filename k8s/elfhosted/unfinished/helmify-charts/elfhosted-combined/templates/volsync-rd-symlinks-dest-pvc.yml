apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "elfhosted-combined.fullname" . }}-volsync-rd-symlinks-dest
  labels:
    app.kubernetes.io/created-by: volsync
  {{- include "elfhosted-combined.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.volsyncRdSymlinksDest.storageRequest | quote }}
  storageClassName: {{ .Values.pvc.volsyncRdSymlinksDest.storageClass | quote }}
