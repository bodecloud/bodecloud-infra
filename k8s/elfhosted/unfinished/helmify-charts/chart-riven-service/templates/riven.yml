apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-riven-service.fullname" . }}-riven
  labels:
    helm.toolkit.fluxcd.io/name: brunner56
    helm.toolkit.fluxcd.io/namespace: my-media-stack
  {{- include "chart-riven-service.labels" . | nindent 4 }}
  annotations:
    meta.helm.sh/release-name: brunner56
    meta.helm.sh/release-namespace: my-media-stack
spec:
  type: {{ .Values.riven.type }}
  selector:
    app.elfhosted.com/name: riven
    app.kubernetes.io/instance: brunner56
    {{- include "chart-riven-service.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.riven.ports | toYaml | nindent 2 }}
