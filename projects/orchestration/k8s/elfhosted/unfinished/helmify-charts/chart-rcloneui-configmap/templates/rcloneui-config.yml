apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chart-rcloneui-configmap.fullname" . }}-rcloneui-config
  labels:
    helm.toolkit.fluxcd.io/name: brunner56
    helm.toolkit.fluxcd.io/namespace: my-media-stack
  {{- include "chart-rcloneui-configmap.labels" . | nindent 4 }}
data:
  TZ: {{ .Values.rcloneuiConfig.tz | quote }}
  config.yaml: {{ .Values.rcloneuiConfig.configYaml | toYaml | indent 1 }}
