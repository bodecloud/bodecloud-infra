apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "elfhosted-neat-final.fullname" . }}-transcode-1g
  labels:
    helm.toolkit.fluxcd.io/name: brunner56
    helm.toolkit.fluxcd.io/namespace: my-media-stack
  {{- include "elfhosted-neat-final.labels" . | nindent 4 }}
  annotations:
    meta.helm.sh/release-name: brunner56
    meta.helm.sh/release-namespace: my-media-stack
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.transcode1G.storageRequest | quote }}
  storageClassName: {{ .Values.pvc.transcode1G.storageClass | quote }}
