apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "elfhosted-neat-final.fullname" . }}-rclone
  labels:
    helm.toolkit.fluxcd.io/name: brunner56
    helm.toolkit.fluxcd.io/namespace: my-media-stack
    velero.io/exclude-from-backup: "true"
  {{- include "elfhosted-neat-final.labels" . | nindent 4 }}
  annotations:
    meta.helm.sh/release-name: brunner56
    meta.helm.sh/release-namespace: my-media-stack
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.pvc.rclone.storageRequest | quote }}
  selector:
    matchLabels:
      name: rclone-brunner56-rclone
  storageClassName: {{ .Values.pvc.rclone.storageClass | quote }}
