# Kept for local discovery; GitHub Actions only loads workflows from the
# repository root. See `/.github/workflows/synthora-ci.yml`.
name: Synthora CI (local pointer)

on:
  workflow_dispatch:

jobs:
  point-to-root:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use /.github/workflows/synthora-ci.yml"
